<ContentDialog
    x:Class="ToolBox.Views.ContextMenuEditorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ToolBox.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="新建自定义右键菜单"
    PrimaryButtonText="创建"
    CloseButtonText="取消"
    DefaultButton="Primary"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <StackPanel Spacing="12" Width="400">
        <TextBox x:Name="MenuNameInput" Header="菜单显示名称" PlaceholderText="例如：用 Notepad++ 打开" />
        
        <TextBox x:Name="MenuCommandInput" Header="执行命令行" PlaceholderText="例如：&quot;C:\path\to\app.exe&quot; &quot;%1&quot;" />
        <TextBlock Text="提示：参数 %1 代表被选中的文件/文件夹路径" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,-8,0,0" />

        <ComboBox x:Name="MenuScopeInput" Header="作用范围" HorizontalAlignment="Stretch" SelectedIndex="0">
            <ComboBoxItem Content="全局文件右键" />
            <ComboBoxItem Content="文件夹图标右键" />
            <ComboBoxItem Content="文件夹/桌面空白处右键" />
        </ComboBox>

        <TextBox x:Name="MenuIconInput" Header="菜单图标路径（可选）" PlaceholderText="可填 .exe 或 .ico 绝对路径" />
    </StackPanel>
</ContentDialog>
