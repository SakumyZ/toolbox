<Window
    x:Class="ToolBox.Views.NotificationWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="通知">
    <Grid
        x:Name="NotificationRoot"
        Background="Transparent"
        PointerEntered="NotificationRoot_PointerEntered"
        PointerExited="NotificationRoot_PointerExited">
        <Border
            x:Name="RootBorder"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="Transparent"
            BorderThickness="0"
            CornerRadius="0">
            <Grid ColumnDefinitions="96,*">
                <Border
                    Grid.Column="0"
                    x:Name="ImageColumnBorder"
                    Background="{ThemeResource AccentFillColorDefaultBrush}"
                    CornerRadius="0">
                    <Grid>
                        <Border x:Name="ImageOverlay" Background="#22000000" />
                        <Image
                            x:Name="PreviewImage"
                            Margin="12"
                            Stretch="Uniform" />
                    </Grid>
                </Border>
                <Grid Grid.Column="1" Padding="14,12,12,10" RowDefinitions="Auto,*,Auto">
                    <Grid ColumnDefinitions="*,Auto">
                        <TextBlock
                            x:Name="TitleText"
                            FontSize="15"
                            FontWeight="SemiBold"
                            MaxLines="1"
                            TextTrimming="CharacterEllipsis"
                            Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                        <Button
                            Grid.Column="1"
                            Width="24"
                            Height="24"
                            Padding="0"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            BorderThickness="0"
                            IsTabStop="False"
                            AllowFocusOnInteraction="False"
                            UseSystemFocusVisuals="False"
                            FocusVisualPrimaryThickness="0"
                            FocusVisualSecondaryThickness="0"
                            Click="CloseButton_Click">
                            <Button.Resources>
                                <Thickness x:Key="ButtonBorderThemeThickness">0</Thickness>
                                <Thickness x:Key="ButtonBorderThemeThicknessFocused">0</Thickness>
                                <SolidColorBrush x:Key="ButtonBorderBrush" Color="Transparent" />
                                <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="Transparent" />
                                <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="Transparent" />
                                <SolidColorBrush x:Key="ButtonBorderBrushDisabled" Color="Transparent" />
                                <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#1A888888" />
                                <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#33888888" />
                            </Button.Resources>
                            <FontIcon Glyph="&#xE711;" FontSize="10" />
                        </Button>
                    </Grid>
                    <TextBlock
                        x:Name="MessageText"
                        Grid.Row="1"
                        Margin="0,4,0,6"
                        FontSize="13"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        MaxLines="2"
                        TextWrapping="WrapWholeWords" />
                    <TextBlock
                        x:Name="FooterText"
                        Grid.Row="2"
                        FontSize="11"
                        Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>